apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv1-rwo
  namespace: parallel
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeMode: Filesystem

