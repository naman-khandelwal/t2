apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv1-rox
  namespace: parallel
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeMode: Filesystem

